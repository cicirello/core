jdk:
  - openjdk17
before_install:
  - sdk install java 17.0.2-tem
  - sdk use java 17.0.2-tem
  - sdk install maven
  - mvn -v
install:
  #- echo $VERSION
  #- [[ "$VERSION" =~ ^(0|[1-9]\d*)\.(0|[1-9]\d*)\.(0|[1-9]\d*)(?:-((?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\.(?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\+([0-9a-zA-Z-]+(?:\.[0-9a-zA-Z-]+)*))?$ ]] && mvn versions:set -DnewVersion=${VERSION} versions:commit
  #- [[ "$VERSION" =~ ^v(0|[1-9]\d*)\.(0|[1-9]\d*)\.(0|[1-9]\d*)(?:-((?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\.(?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\+([0-9a-zA-Z-]+(?:\.[0-9a-zA-Z-]+)*))?$ ]] && mvn versions:set -DnewVersion=${VERSION:1} versions:commit
  - mvn install -Dmaven.javadoc.skip=true -DskipTests
